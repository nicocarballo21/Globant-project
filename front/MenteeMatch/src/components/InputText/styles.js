import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    width: '80%',
    height: 50,
    padding: 15,
    borderColor: 'white',
    borderBottomWidth: 1,
    marginBottom: 20,
  },
});

export default styles;
